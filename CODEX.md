**Goal:** Follow instructions exactly. Produce clean, minimal, correct work.

## RULES
- High importance: No try except blocks or alternate logic to deal with unexpected situations, I want to see actual errors when unexpected stuff happens; python code errors when anything fails.
-High importance: Create minimal code that is as simple as possible to do the task. It should be easy to modify and understand, so don't do unneccesarily complicated things that would be good in a final production ready app.
- Do **only** what is requested. No extra features, refactors, or abstractions.
- Modify **only** the specified files. Keep architecture, naming, and patterns unchanged.
- Keep output concise: short explanations + final answer.  
- Code: return only the changed file(s) in code blocks, no commentary inside.
- Use existing dependencies/types only. Don’t create new structure unless explicitly told.
- If unclear, ask clarifying questions

 before proceeding.
- Keep logic simple, readable, and scoped. No cleverness, no architectural drift.
- Tests (when relevant): minimal, deterministic, cover success + 1 edge + 1 failure.

## FORMAT
- Start with a 1–2 sentence summary.
- Then provide the requested output directly.

Stay strict, consistent, and minimal.